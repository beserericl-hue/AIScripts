fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.5.2
httpx==0.27.2
python-multipart==0.0.12
anthropic==0.39.0
openai==1.54.3
qdrant-client==1.12.1
pymongo==4.10.1
PyJWT==2.9.0
prometheus-client==0.21.0
mammoth==1.8.0
markdownify==0.13.1
beautifulsoup4==4.12.3
boto3==1.35.81
python-docx==1.1.2
pypdf==5.1.0
# Office-format text extraction for bulk supporting-evidence placement
# (xlsx enrollment/assessment tables, pptx program decks). Read-only, values
# only — used to match a dropped file against the imported narratives.
openpyxl==3.1.5
python-pptx==1.0.2
# OCR for scanned appendix PDFs (MCC narrative import). PyMuPDF rasterizes pages
# without poppler; pytesseract shells out to the tesseract-ocr system binary
# (installed in the Docker image). Pillow bridges the two.
pymupdf==1.24.14
pytesseract==0.3.13
Pillow==11.0.0
